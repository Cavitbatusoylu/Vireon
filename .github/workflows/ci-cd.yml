name: Vireon CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET 8.0
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore Vireon.PresentationLayer/Vireon.PresentationLayer.csproj

    - name: Build
      run: dotnet build Vireon.PresentationLayer/Vireon.PresentationLayer.csproj --no-restore --configuration Release

    - name: Publish
      run: dotnet publish Vireon.PresentationLayer/Vireon.PresentationLayer.csproj --configuration Release --output ./publish

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: vireon-app
        path: ./publish
